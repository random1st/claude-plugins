{
  "name": "delegate",
  "version": "0.2.0",
  "description": "Unified external AI delegation — route to Codex, Gemini, or Grok based on cost/quality tradeoffs. Uses raw CLI binaries (codex exec, gemini, grok -p)."
}
